#include <cmath>
#include <cstdio>
#include <iostream>
using namespace std;
int special_num(int m, int n){
    for(int i = m; i<=n ; i++){
        int x = (i/10) + (i%10);
        int y = (i/10) * (i%10);
        int z = x + y;
        if(z==i){
            cout<<i<<"\n";
        }
    }
   return 0;
}


int main() {
    int m,n;
    cin>>m>>n;
    special_num(m,n);
    return 0;
}
